const request = require('supertest')("http://localhost:8888");
const cheerio = require('cheerio');
const should = require('should');


function rnd_it(s, f) {
    //speed up the test
    if (Math.random()<0.01) {
        it(s,f);
    }
}


describe('Task 5: Check win/tie', function() {

    it('Web server should be started, listening on port 8888', function(done) {

        request
            .get('/')
            .send()
            .end(done);

    });

    describe('GET /tic-tac-toe/{s} - tie', function() {

        it('GET /tic-tac-toe/XXOOXXXOO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XXOOXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XXOOOXXXO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XXOOOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XXOOOXXOX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XXOOOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XXOOOXXOO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XXOOOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XOXXXOOXO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XOXXXOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XOXXOXOXO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XOXXOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XOXXOOOXX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XOXXOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XOXXOOOXO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XOXXOOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XOXOXXOXO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XOXOXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XOXOXOOXO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XOXOXOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XOXOOXXXO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XOXOOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XOXOOXOXO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XOXOOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XOOOXXXXO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XOOOXXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XOOOXXXOO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XOOOXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XOOOXXOXO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XOOOXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/XOOOOXXXO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/XOOOOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OXXXXOOOX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OXXXXOOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OXXXOOXOX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OXXXOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OXXXOOOXX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OXXXOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OXXXOOOOX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OXXXOOOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OXOXXOXOX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OXOXXOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OXOXXOOOX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OXOXXOOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OXOXOXXOX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OXOXOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OXOXOOXOX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OXOXOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OXOOXXXOX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OXOOXXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OXOOXXXOO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OXOOXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OXOOXOXOX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OXOOXOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OXOOOXXOX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OXOOOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OOXXXOOXX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OOXXXOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OOXXXOOXO returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OOXXXOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OOXXXOOOX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OOXXXOOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        it('GET /tic-tac-toe/OOXXOOOXX returns 200 OK, with an HTML page that represents a tie board', function(done) {

            request
                .get('/tic-tac-toe/OOXXOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text().toLowerCase();

                        should(false).be.equal(body.indexOf("win") >= 0);
                        should(true).be.equal(body.indexOf("tie") >= 0);

                        should(false).be.equal(msg.indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("tie") >= 0);

                        done();

                    }
                });

        });

    });


    describe('GET /tic-tac-toe/{s} - O win', function() {

        rnd_it('GET /tic-tac-toe/____XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/____XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/___X_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/___X_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/___XX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/___XX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/___OOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/___OOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/___OOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/___OOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/___OOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/___OOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/___OOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/___OOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__X__XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__X__XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__X_X_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__X_X_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__X_XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__X_XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XX__OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XX__OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XX_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XX_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XXX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XXX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XXXOOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XXXOOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XXOXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XXOXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XOXXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XOXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XOOO__X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XOOO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XOOO_X_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XOOO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XOOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XOOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XOOOX__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XOOOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XOOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XOOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XOOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XOOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XOOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XOOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XOOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XOOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XOOOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XOOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__XOOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__XOOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__O__OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__O__OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__O_XO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__O_XO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__O_XOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__O_XOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__O_XOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__O_XOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__O_O_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__O_O_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__O_OXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__O_OXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__O_OXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__O_OXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__O_OXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__O_OXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OX_O_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OX_O_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OX_OX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OX_OX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OX_OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OX_OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXXO__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXXO__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXXO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXXO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXXOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXXOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXXOXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXXOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXXOOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXXOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXO_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXO_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXO_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXO_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXOXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXOXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OXOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OXOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OOXOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OOXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OOOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OOOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/__OOOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/__OOOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X___XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X___XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X__X_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X__X_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X__XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X__XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_X__OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_X__OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_X_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_X_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_XX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_XX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_XXOOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_XXOOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_XOXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_XOXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_OXXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_OXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_OOO__X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_OOO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_OOO_X_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_OOO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_OOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_OOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_OOOX__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_OOOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_OOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_OOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_OOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_OOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_OOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_OOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_OOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_OOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_OOOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_OOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_X_OOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_X_OOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XX___OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XX___OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XX__XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XX__XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XX_X_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XX_X_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XX_XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XX_XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XX_XOOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XX_XOOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XX_OXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XX_OXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXX__OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXX__OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXX_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXX_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXX_OOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXX_OOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXXX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXXX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXXXOOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXXXOOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXXO_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXXO_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXXOXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXXOXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXO_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXO_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOXXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOO___ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOO___')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOO__X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOO_X_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOO_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOOX__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOOXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOOO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOOOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XXOOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XXOOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO__O_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO__O_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO__OX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO__OX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO__OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO__OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_XO__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_XO__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_XOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_XOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_XOXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_XOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_O_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_O_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_O_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_O_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_O_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_O_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_OXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_OXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_OXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_OXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_OXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_OXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_OXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_OXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_OXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_OXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_OXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_OXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_OOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_OOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XO_OOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XO_OOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOX_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOX_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOX_O__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOX_O__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOX_O_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOX_O_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOX_OX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOX_OX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOX_OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOX_OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOX_OXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOX_OXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOX_OOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOX_OOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXXO__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXXO__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXXO_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXXO_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXXOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXXOXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXXOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXXOO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXXOO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXO_O__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXO_O__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXO_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXO_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXO_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXO_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXO_OXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXO_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXO_OOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXO_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOXO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOXOO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOOO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOOOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOXOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOXOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOO_OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOO_OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOOXOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOOXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOOO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOOO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOOOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOOOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOOOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOOOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOOOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOOOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOOOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOOOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOOOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOOOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOOOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOOOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_XOOOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_XOOOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O__O_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O__O_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O__OX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O__OX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O__OXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O__OXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O__OXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O__OXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O_XO__OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O_XO__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O_XO_XO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O_XO_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O_XO_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O_XO_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O_XOX_O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O_XOX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O_XOX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O_XOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O_XOXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O_XOXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O_XOXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O_XOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O_XOXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O_XOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O_XOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O_XOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O_XOOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O_XOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O_OOXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O_OOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_O_OOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_O_OOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OX_XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OX_XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OX_O__OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OX_O__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OX_O_XO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OX_O_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OX_O_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OX_O_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OX_OX_O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OX_OX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OX_OX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OX_OX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OX_OXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OX_OXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OX_OXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OX_OXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OX_OXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OX_OXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OX_OXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OX_OXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OX_OOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OX_OOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXX_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXX_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXO__O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXO__O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXO__OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXO__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXO_XO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXO_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXO_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXO_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXO_XOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXO_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXO_OOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXO_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXOX_O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXOX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXOX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXOX_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXOX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXOXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXOXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXOXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXOXOO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXOXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXOO_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXOO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXOOXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXOOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXXOOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXXOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXOO_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXOO_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXOOX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXOOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXOOXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXOOXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXOOXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXOOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXOOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXOOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXOOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXOOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXOOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXOOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OXOOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OXOOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OO_XOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OO_XOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OO_OXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OO_OXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OO_OXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OO_OXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OOX_OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OOX_OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OOXXO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OOXXO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OOXXOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OOXXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OOXXOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OOXXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OOXO_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OOXO_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OOXO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OOXO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OOXOX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OOXOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OOXOXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OOXOXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OOXOXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OOXOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OOXOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OOXOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OOXOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OOXOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/_OOXOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/_OOXOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X____XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X____XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X___X_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X___X_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X___XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X___XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__X__OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__X__OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__X_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__X_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__XX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__XX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__XXOOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__XXOOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__XOXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__XOXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__OXXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__OXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__OOO__X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__OOO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__OOO_X_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__OOO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__OOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__OOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__OOOX__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__OOOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__OOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__OOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__OOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__OOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__OOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__OOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__OOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__OOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__OOOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__OOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X__OOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X__OOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_X___OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_X___OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_X__XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_X__XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_X_X_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_X_X_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_X_XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_X_XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_X_XOOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_X_XOOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_X_OXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_X_OXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XX__OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XX__OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XX_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XX_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XX_OOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XX_OOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XXX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XXX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XXXOOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XXXOOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XXO_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XXO_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XXOXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XXOXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XO_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XO_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOXXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOO___ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOO___')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOO__X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOO_X_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOO_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOOX__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOOXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOOO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOOOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_XOOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_XOOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O__O_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O__O_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O__OX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O__OX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O__OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O__OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_XO__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_XO__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_XO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_XO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_XOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_XOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_XOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_XOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_XOXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_XOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_XOOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_XOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_O_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_O_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_O_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_O_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_O_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_O_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_OXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_OXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_OXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_OXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_OXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_OXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_OXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_OXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_OXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_OXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_OXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_OXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_OOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_OOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_O_OOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_O_OOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OX_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OX_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OX_O__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OX_O__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OX_O_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OX_O_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OX_OOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OX_OOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXXO__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXXO__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXXO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXXO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXXO_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXXO_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXXOO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXXOO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXXOOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXXOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXO_O__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXO_O__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXO_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXO_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXO_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXO_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXO_OXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXO_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXO_OOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXO_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXOXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXOXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXOXO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXOXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXOXOO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXOXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXOO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXOO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXOOO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXOOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXOOOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXOOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OXOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OXOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OO_OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OO_OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OOXO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OOXO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OOXOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OOXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OOXOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OOXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OOO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OOO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OOOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OOOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OOOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OOOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OOOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OOOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OOOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OOOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OOOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OOOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OOOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OOOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/X_OOOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/X_OOOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX____OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX____OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX___XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX___XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX__X_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX__X_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX__XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX__XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX__XOOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX__XOOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX__OXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX__OXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_X__OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_X__OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_X_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_X_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_X_OOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_X_OOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_XX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_XX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_XXOOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_XXOOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_XO_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_XO_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_XOXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_XOXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_O_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_O_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OXXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOO___ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOO___')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOO__X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOO_X_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOO_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOOX__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOOXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOOO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOOOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XX_OOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XX_OOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO__XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO__XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO__O__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO__O__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO__O_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO__O_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO__OX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO__OX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO__OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO__OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO__OXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO__OXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO__OOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO__OOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_X_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_X_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_XO__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_XO__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_XO_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_XO_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_XOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_XOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_XOXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_XOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_XOO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_XOO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_O_O__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_O_O__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_O_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_O_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_O_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_O_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_O_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_O_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_O_OXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_O_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_O_OOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_O_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OXO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OXOO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OOO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OOOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXO_OOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXO_OOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOX__OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOX__OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOX_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOX_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOX_O__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOX_O__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOX_O_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOX_O_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOX_O_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOX_O_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOX_OO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOX_OO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOX_OOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOX_OOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXXO__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXXO__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXXO_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXXO_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXXOO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXXOO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXXOOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXXOOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXO_O__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXO_O__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXO_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXO_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXO_O_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXO_O_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXO_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXO_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXO_OXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXO_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXO_OO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXO_OO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXO_OOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXO_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOXO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOXOO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOO__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOO__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOOO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOOO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOOO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOOOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOOOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOXOOOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOXOOOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOO_O_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOO_O_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOO_OX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOO_OX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOO_OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOO_OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOXXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOXO__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOXO__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOXOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOXOXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOXOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOO_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOO_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOO_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOO_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOO__X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOO_X_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOOX__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XXOOOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XXOOOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO__XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO__XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO__O__OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO__O__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO__O_XO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO__O_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO__O_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO__O_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO__OX_O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO__OX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO__OX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO__OX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO__OXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO__OXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO__OXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO__OXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO__OXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO__OXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO__OXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO__OXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO__OOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO__OOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_X_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_X_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XO__O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XO__O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XO__OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XO__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XO_OOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XO_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOX_O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOX_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOXOO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOO_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OO_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OO_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OOX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OOXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OOXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OOXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX__XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX__XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_X_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_X_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_O__O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_O__O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_O__OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_O__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_O_XO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_O_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_O_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_O_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_O_XOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_O_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_O_OOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_O_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_OX_O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_OX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_OX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_OX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_OX_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_OX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_OXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_OXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_OXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_OXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_OXOO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_OXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_OXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_OXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_OO_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_OO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_OOXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_OOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_OOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_OOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXX__OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXX__OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXX_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXX_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXXOOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXXOOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXO__O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXO__O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXO__OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXO__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXO__OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXO__OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXO_OO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXO_OO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXO_OOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXO_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOX_O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOX_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOXOO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOO_O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOO_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOO_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOOOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOOOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOO__OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOO__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOO_XO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOO_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOO_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOO_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOX_O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOO__X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOO_X_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOOX__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO__OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO__OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_O_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_O_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_O_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_O_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_OX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_OX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_OXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_OXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_OXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_OXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_OXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_OXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_OXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_OXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_OXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_OXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX_O_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX_O_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXO__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXO__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXOOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXO__OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXO__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXO_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXO_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXO_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXO_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOX_O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOOXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O___O_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O___O_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O___OX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O___OX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O___OXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O___OXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O___OXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O___OXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XO__XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XO__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XO_X_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XO_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XO_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XO_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XOX__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XOX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XOX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XOX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XOXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XOXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XOXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XOXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__O__OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__O__OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__O_XO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__O_XO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__O_XOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__O_XOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__O_XOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__O_XOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OX_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OX_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OX_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OX_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OX_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OX_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OXXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OXXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OXXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OXXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OXXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OXXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OXXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OXXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OXXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OXXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OXOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OXOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OOXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_O__XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_O__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_O_X_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_O_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_O_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_O_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OX__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XX_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XX_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXO___O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXO___O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXO__XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXO__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXO_X_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXO_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXO_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXO_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXO_XOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXO_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXO_OXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXO_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOX__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOX_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOXO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO__O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO__O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO__OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO__OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO__OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO__OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO_XO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO_XO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO_XO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO_XO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO_XOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO_XOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO_XOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO_XOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO_XOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO_XOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO_XOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO_XOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO_OOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO_OOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOX_O__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOX_O__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOX_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOX_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOX_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOX_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOX_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOX_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOX_OXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOX_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOX_OOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOX_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXXO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXXOO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXOO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXOOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOO_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOO_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_O_XOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_O_XOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_O_OXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_O_OXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_O_OXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_O_OXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OX_OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OX_OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXO_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXO_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXOX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXOX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXOXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXOXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXOXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OO_XOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OO_XOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OOX_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OOX_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OOXXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OOXXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OOXXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OOXXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OOXXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OOXXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__O__XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__O__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__O_X_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__O_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__O_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__O_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__OX__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__OX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__OX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__OX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__OXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__OXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__OXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__OXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__OXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__OXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__OXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__OXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__OOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__OOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_X_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_X_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XO___O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XO___O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XO__XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XO__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XO_X_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XO_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XO_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XO_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XO_XOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XO_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XO_OXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XO_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XOX__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XOX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XOX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XOX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XOX_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XOX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XOXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XOXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XOXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XOXO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XOXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XOO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XOO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XOOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XOOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_O__O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_O__O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_O__OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_O__OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_O__OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_O__OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_O_XO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_O_XO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_O_XO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_O_XO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_O_XOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_O_XOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_O_XOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_O_XOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_O_XOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_O_XOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_O_XOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_O_XOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_O_OOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_O_OOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OX_O__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OX_O__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OX_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OX_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OX_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OX_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OX_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OX_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OX_OXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OX_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OX_OOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OX_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXXO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXXOO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXOO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXOOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OO_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OO_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OOX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OOX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OOXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OOXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OOXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX__XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX__XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_X_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_X_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_XXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_XXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_O___O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_O___O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_O__XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_O__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_O_X_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_O_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_O_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_O_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_O_XOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_O_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_O_OXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_O_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OX__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OX_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OXO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXX__OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXX__OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXX_XOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXX_XOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXX_OOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXX_OOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXXOOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXXOOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXO___O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXO___O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXO__XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXO__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXO__OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXO__OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXO_X_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXO_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXO_XOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXO_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXO_O_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXO_O_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXO_OXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXO_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOX__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOX_OO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOXO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOO__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOO__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOO_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOOXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOOOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO__O__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO__O__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO__O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO__O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO__OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO__OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO__OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO__OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO__OXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO__OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO__OOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO__OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO_XO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO_XO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO_XO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO_XO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO_XO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO_XO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO_XOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO_XOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO_XOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO_XOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO_XOO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO_XOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO_XOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO_XOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO_OO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO_OO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO_OOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO_OOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO_OOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO_OOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOX_O__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOX_O__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOX_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOX_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOX_O_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOX_O_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOX_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOX_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOX_OXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOX_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOX_OO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOX_OO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOX_OOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOX_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXXO_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXXOO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXXOOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXOO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXOO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXOO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXOOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXOOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXOOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXOOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOO__XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOO__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOO_X_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOO_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOO_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOO_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOO_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOO_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOO_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOO_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOX__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOO__X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOO_X_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOOX__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO__OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO__OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_XOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_XOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_O_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_O_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_O_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_O_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_OX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_OX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_OXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_OXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_OXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_OXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_OXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_OXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_OXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_OXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_OXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_OXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOX_O_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOX_O_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOX_OX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOX_OX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOX_OXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOX_OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXO__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXO__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXOX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXOXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXO__XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXO__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXO_X_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXO_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXO_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXO_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXO_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXO_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXO_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXO_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXO_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXO_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOX__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOO__OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOO__OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOO_XO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOO_XO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOO_XOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOO_XOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOO_XOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOO_XOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOX_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOX_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOX_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOX_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOX_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOX_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOOXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO__OXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO__OXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO__OXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO__OXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XO_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XO_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XO_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XO_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XOX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XOX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XOX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XOXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XOXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XOXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XOXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XOXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XOXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_O_XOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_O_XOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_OX_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_OX_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_OXXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_OXXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_OXXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_OXXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_OXXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_OXXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_O_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_O_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_O_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_O_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_OX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_OX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_OX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_OX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_OXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_OXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_OXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_OXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_OXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_OXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_OXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_OXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXO__XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXO__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXO__OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXO__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXO_X_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXO_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXO_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXO_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXO_XO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXO_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXO_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXO_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOX__O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOX_O_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOXXOO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOOXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOOXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXO__OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXO__OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXO_XO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXO_XO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXO_XOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXO_XOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXO_XOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXO_XOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOX_O_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOX_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOX_OX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOX_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOX_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOX_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXXO__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXXOXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXXOOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXOOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOOXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOOXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOOXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOOOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO____XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO____XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO___X_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO___X_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO___XX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO___XX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO___XXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO___XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO__X__X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO__X__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO__X_X_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO__X_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO__X_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO__X_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO__XX__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO__XX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO__XX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO__XX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO__XXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO__XXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO__XXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO__XXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO__XXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO__XXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO__XXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO__XXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO__XOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO__XOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO__OXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO__OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_X___X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_X___X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_X__X_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_X__X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_X__XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_X__XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_X_X__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_X_X__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_X_X_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_X_X_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_X_XX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_X_XX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_X_XXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_X_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_X_XXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_X_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_X_XOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_X_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_X_OXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_X_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XX___ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XX___')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XX__X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XX__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XX_X_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XX_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XX_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XX_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XX_XO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XX_OX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XXX__ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XXX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XXX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XXX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XXX_O returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XXXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XXXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XXXXO returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XXXO_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XXXOX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XXO_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XXOX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XXOXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XO_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XOX_X returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XOXX_ returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_XOXXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_XOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_O_XXX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO_O_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("O") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOO_OX_XX returns 200 OK, with an HTML page that represents a board with O as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOO
O_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_X__XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_X__XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_X__XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_X_XXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_X_XXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_X_OX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_X_OX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_X_OX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_X_OX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_X_OXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_X_OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_X_OXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_X_OXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_X_OXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_X_OXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_X_OXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_X_OXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XX_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XX_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XX_OOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XX_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XXX_OO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XXX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XXXO_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XXXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XXXOO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XXXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XXXOOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XXO_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XXO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XXOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XXOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XXOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XXOXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XXOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XXOO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XXOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XXOOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XXOOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XO_X__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XO_X__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XO_X_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XO_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XO_XXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XO_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XO_XO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XO_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XO_XOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XO_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XO_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XO_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOXX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOXXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOOX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOOX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOOXX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_XOOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_XOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_O_OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_O_OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OX___X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OX___X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OX__OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OX__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OX_XOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OX_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OX_O_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OX_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OX_OXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OX_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OX_OOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OX_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OXX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OXX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OXXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OXXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OXXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OXO__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OXO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OXO_XX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OXO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OXO_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OXO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OXOX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OXOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OXOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OXOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OXOO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OXOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OXOOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OXOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XO_OO_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XO_OO_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX__XOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX__XOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_X_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_X_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_X_OOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_X_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_XO_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_XO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_XOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_XOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_XOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_XOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_XOXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_XOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_XOO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_XOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_XOOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_XOOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOX_OXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOX_OXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXX__XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXX__XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXX_OX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXX_OX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXX_OXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXX_OXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXX_OXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXX_OXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXXXOOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXXOXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXXOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXXOOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXXOOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXO_X_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXO_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXO_XO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXO_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXO_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXO_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOOX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXXOOXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXXOOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXO_X_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXO_X_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXO_XO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXO_XO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXO_XOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXO_XOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOX__OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOX__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOX_X_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOX_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOX_XO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOX_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOX_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOX_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOX_O_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOX_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOX_OOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOX_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXO__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXO_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXOX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXOXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXOO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXOOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOXOOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOXOOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOX__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOX__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOXOOXOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOXOOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO__OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO__OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_X___X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_X___X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_X__OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_X__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_X_XOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_X_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_X_O_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_X_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_X_OXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_X_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_X_OOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_X_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XO__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XO_XX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XO_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XOX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XOO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_XOOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_XOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_O_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_O_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOO_OOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOO_OOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX__X__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX__X__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX__X_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX__X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX__XXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX__XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX__XO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX__XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX__XOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX__XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX__XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX__XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX_XX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX_XX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX_XXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX_XXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX_XXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX_XXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX_OX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX_OX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX_OX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX_OX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX_OX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX_OX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX_OXX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX_OXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX_OXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX_OXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX_OXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX_OXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOX_OXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOX_OXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXX__OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXX__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXX_X_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXX_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXX_XO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXX_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXX_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXX_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXX_O_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXX_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXX_OOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXX_OOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXX__O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXX_O_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXX_OO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXXO__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXXO_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXXOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXXOO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXXOOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXO__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXO_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXO_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXOX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXOXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXOO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXOO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXOOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXXOOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXXOOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXO_X__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXO_X__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXO_X_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXO_X_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXO_X_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXO_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXO_XX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXO_XX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXO_XXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXO_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXO_XO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXO_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXO_XOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXO_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOXX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOXX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOXX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOXXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOXXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOXXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOOX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOOX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOOXX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOXOOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOXOOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOO__XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOO__XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOO_OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOO_OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOX___X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOX___X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOX__XX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOX__XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOX__OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOX__OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOX_X_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOX_X_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOX_XOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOX_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOX_O_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOX_O_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOX_OXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOX_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXX__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXX__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXXXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXXOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXO__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXO__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXO_XX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXOX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOXOOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOXOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOO_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOO_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/XOOOOXXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/XOOOOXXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O____OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O____OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O___O_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O___O_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O___OOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O___OOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XXX__O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XXX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XXX_O_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XXX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XXX_OO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XXX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XXXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XXXO__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XXXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XXXO_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XXXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XXXOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XXXOO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XXXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XXXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XXXOOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__XOOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__XOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__O__XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__O__XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__O_OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__O_OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OXOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OXOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OO_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OO_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O__OOXXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O__OOXXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X__X_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X__X_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X__XO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X__XO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X__XOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X__XOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_X_X_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_X_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_X_XO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_X_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_X_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_X_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_XXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_XXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_XXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_XXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_XOX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_XOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_XOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_XOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_XOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_XOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_XOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_XOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_XOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_XOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_XOXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_XOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OX__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OX__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OXXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OXOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_X_OOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_X_OOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XX_XOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XX_XOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXX_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXX_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXXX_OO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXXX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXXXO_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXXXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXXXOO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXXXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXXXOOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXXOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXXOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXXOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXXOXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXXOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XXOXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XXOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO_X__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO_X__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO_X_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO_X_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO_XXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO_XXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XO_OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XO_OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOX_X__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOX_X__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOX_X_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOX_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOX_XXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOX_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOX_XO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOX_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOX_XOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOX_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOX_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOX_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXXX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXXXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXOX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXOX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXOXX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOXOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOXOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOO_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOO_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOX__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOX__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOX_XX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOX_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOXX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOXX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_XOOXXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_XOOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_O___XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_O___XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_O__OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_O__OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_O_XOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_O_XOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_O_O_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_O_O_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_O_OXXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_O_OXXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_O_OOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_O_OOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OX_OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OX_OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXX___ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXX___')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXX__O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXX_XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXX_O_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXX_OO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXXX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXXXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXXO__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXXO_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXXOX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXXOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXXOO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXXXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXO_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXO_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OXOOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OXOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OO__XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OO__XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OO_XXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OO_XXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OO_OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OO_OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OOX_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OOX_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OOXOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OOXOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OOO_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OOO_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/O_OOOXXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/O_OOOXXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__X__XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__X__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__X_OX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__X_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__X_OXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__X_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__XXOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__XXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__XO_X_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__XO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__XO_XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__XO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__XOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__XOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__XOOX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__XOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__XOOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__XOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__XOOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__XOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX__OOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX__OOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XX_OXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XX_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XXX_OO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XXX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XXXO_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XXXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XXXOO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XXXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XXXOOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XXO_XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XXO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XXOOX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XXOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_XXOOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_XXOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_O_OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_O_OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OX__X_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OX__X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OX__XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OX__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OX_XXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OX_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXX_XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXO_X_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXO_XX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXO_XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXOXX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OXOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OX_OO_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OX_OO_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX__XOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX__XOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_X_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_X_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_X_OXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_X_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_XO_XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_XO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_XOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_XOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_XOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_XOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_XOXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_XOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_XOOX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_XOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_XOOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_XOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXX_OXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXX_OXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXXXOOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXXOXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXXOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXXOOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXXOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXXOXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXXOXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXO_X_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXO_X_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOX__XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOX__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOX_X_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOX_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOX_XO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOX_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOX_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOX_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXO_X_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXO_XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXOX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOXOXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOXOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOX__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOX__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXXOOXXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXXOOXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO__OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO__OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_X__X_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_X__X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_X__XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_X__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_X_XXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_X_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_X_OX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_X_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_X_OXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_X_OXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_X_OXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_X_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_XX_XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_XX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_XXOX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_XXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_XXOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_XXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_XO_X_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_XO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_XO_XX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_XO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_XO_XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_XO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_XOXX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_XOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_XOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_XOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_XOOX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_XOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_XOOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_XOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_O_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_O_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXO_OOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXO_OOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXX__XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXX__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXX_OX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXX_OX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXX_OXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXX_OXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXX__O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXX_O_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXX_OO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXXO__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXXO_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXXOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXXOO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXXOOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXO_X_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXO_XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXO_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXOOX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXOOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXOOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXOOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXXOOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXXOOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOXOOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOXOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOO__XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOO__XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOO_OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOO_OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOX__X_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOX__X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOX__XX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOX__XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOX__XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOX__XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOX_XX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOX_XX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOX_XXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOX_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXX_X_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXX_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXX_XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXXXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXO_X_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXO_X_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXO_XX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXO_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXOXX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOXOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOO_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOO_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OXOOOXXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OXOOOXXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO____XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO____XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO___OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO___OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO__XOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO__XOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO__O_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO__O_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO__OXXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO__OXXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO__OOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO__OOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_X_OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_X_OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXX___ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXX___')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXX__O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXX_XO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXX_XO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXX_O_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXX_OO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXXX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXXXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXXO__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXXO_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXXOX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXXOX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXXOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXXOO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XXXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XO_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XO_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_XOOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_XOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_O__XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_O__XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_O_XXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_O_XXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_O_OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_O_OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_OX_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_OX_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_OXOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_OXOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_OO_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_OO_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OO_OOXXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OO_OOXXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX__X__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX__X__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX__X_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX__X_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX__XXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX__XXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX__XO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX__XO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX__XOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX__XOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX__XOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX__XOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX__OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX__OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_X_X__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_X_X__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_X_X_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_X_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_X_XXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_X_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_X_XO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_X_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_X_XOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_X_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_X_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_X_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XXX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XXXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XOX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XOX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XOXX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_XOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_XOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_O_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_O_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_OX__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_OX__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_OX_XX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_OX_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_OXX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_OXX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_OXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_OXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_OXOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_OXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOX_OOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOX_OOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXX_X_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXX_X_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXX_XO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXX_XO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXX_XOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXX_XOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXX_X_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXX_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXX_XO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXX_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXX_XOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXX_XOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXX__O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXX__O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXX_O_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXX_O_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXX_OO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXX_OO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXXO__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXXO__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXXO_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXXO_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXXOXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXXOXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXXOO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXXOO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXXOOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXXOOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXXOOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXXOOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXOX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXOX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXOX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXOXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXOXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXXOXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXXOXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOX__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOX__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOXO_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOXO_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOXOXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOXOXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXXOOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXXOOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXO__XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXO__XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXO_X__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXO_X__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXO_X_XX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXO_X_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXO_X_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXO_X_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXO_XX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXO_XX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXO_XXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXO_XXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXO_OXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXO_OXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOX_X__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOX_X__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOX_X_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOX_X_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOX_X_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOX_X_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOX_XX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOX_XX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOX_XXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOX_XXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOX_XO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOX_XO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOX_XOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOX_XOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXX__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXX__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXX_OX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXX_OX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXXX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXXX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXXX_O returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXXX_O')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXXXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXXXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXXXO_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXXXO_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXXXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXXXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXXXOO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXXXOO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXOX__ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXOX__')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXOX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXOX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXOXX_ returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXOXX_')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXOXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXOXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXOXXO returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXOXXO')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOXOXOX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOXOXOX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOO_XXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOO_XXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOOX__X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOOX__X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOOX_XX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOOX_XX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOOXX_X returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOOXX_X')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });
        rnd_it('GET /tic-tac-toe/OOXOOXXXX returns 200 OK, with an HTML page that represents a board with X as a winner', function(done) {

            request
                .get('/tic-tac-toe/OOXOOXXXX')
                .send()
                .expect(200)
                .end((err, res) => {
                    if (err) done(err);
                    else {

                        const $ = cheerio.load(res.text);

                        const body = $("body").attr("class").toLowerCase();

                        const msg = $(".message").text();

                        should(true).be.equal(body.indexOf("win") >= 0);
                        should(false).be.equal(body.indexOf("tie") >= 0);

                        should(true).be.equal(msg.toLowerCase().indexOf("win") >= 0);
                        should(true).be.equal(msg.indexOf("X") == 0);
                        should(false).be.equal(msg.toLowerCase().indexOf("tie") >= 0);

                        done();

                    }
                });

        });

    });

});